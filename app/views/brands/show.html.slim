.container
  p#notice = notice
.text-center
  = image_tag @brand.brand_image.url(:medium)
  h1 = @brand.name
  h4 = "Sector Name"
  = image_tag @brand.brand_logo.url(:medium)
  .description = @brand.description
  = link_to 'View More', @brand.url, :class=> 'btn btn-default'
  br
  = link_to 'Edit', edit_brand_path(@brand)
  '|
  = link_to 'Back', brands_path
